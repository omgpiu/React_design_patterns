export const enum Modals {
  Hide = 'Hide Modal',
  Show = 'Show Modal'
}

export const enum Address {
  Layout = '/layout',
  Lists = '/lists',
  Modal = '/modal',
  Container = '/container',
  Forms = '/Forms'

}
